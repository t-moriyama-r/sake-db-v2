# カテゴリ詳細ページ仕様

## アクセス制御

- 認証: **不要**（未ログインでも閲覧可）
- 存在しない ID: `/` へリダイレクト

## UI要素ごとの表示条件

| UI要素 | 未ログイン | ログイン済み |
|---|---|---|
| 編集ボタン | **非表示** | 表示 |
| お酒を追加ボタン | **非表示** | 表示 |
| サブカテゴリを追加ボタン | **非表示** | 表示 |

ログイン状態はサーバーサイドで `getServerUser()` により判定し、`isLoggedIn` として渡す。
